# 함락! 무한의 지옥철 개발 계획

## 1. 목적

이 문서는 `Design.txt`와 이후 논의에서 확정된 게임 규칙을 실제 개발 순서로 옮기기 위한 계획이다.

세부 함수나 모듈 구조보다 다음 질문에 답하는 데 집중한다.

- 무엇을 먼저 만들어야 하는가?
- 그 작업이 왜 선행되어야 하는가?
- 어느 상태가 되어야 다음 단계로 넘어갈 수 있는가?
- 알파 테스트에서 무엇을 검증해야 하는가?

`Design.txt`는 게임 규칙의 기준 문서로, 이 문서는 구현 순서와 검증 기준을 정하는 문서로 사용한다.

## 2. 개발 원칙

### 규칙 판정과 장면 묘사를 분리한다

저항, 은폐, 무드, 카드 이동, 계획 발동과 승패 결과는 모델 요청 전에 게임 엔진이 결정한다. LLM은 결정된 행동과 결과를 하나의 장면으로 묘사하며 새로운 행동이나 결과를 임의로 추가하지 않는다.

이 분리는 같은 카드가 매번 동일하게 작동하도록 하고, 묘사 품질과 게임 밸런스를 서로 독립적으로 개선하기 위해 필요하다.

모델 요청 전에 결정한 결과와 현재 전투 상태에 실제로 반영한 결과는 구분한다. 생성 중에는 결정된 결과를 대기 트랜잭션으로 보관하고, 정상적인 LLM 출력이 도착했을 때 한 번만 현재 상태에 반영한다.

### RisuAI의 정상 생성 경로를 유지한다

LLM 생성을 시작하는 유일한 방법은 사용자가 RisuAI의 전송 버튼을 직접 누르는 것이다. Lua가 저수준 LLM 함수를 직접 호출하거나 전송을 자동화하지 않는다.

프리셋, 캐릭터 설정, 활성 로어북과 RisuAI가 구성하는 정상 프롬프트가 모두 전달되어야 하기 때문이다. Lua는 `onInput`에서 선택을 검증하고 턴 결과를 준비하며, `editRequest`에서 정상 요청에 턴 사건을 추가하고, `onOutput`에서 상태와 UI를 갱신하는 역할만 담당한다.

### 카드보다 공통 규칙을 먼저 만든다

개별 카드가 드로우, 무드 변경, 계획 차단과 같은 규칙을 각자 구현하면 카드가 늘어날수록 예외가 폭증한다. 턴 진행, 무드 계산, 카드 영역, 메커니즘과 트리거를 먼저 확정한 뒤 카드는 그 규칙을 조합하도록 한다.

### 숨겨진 정보의 경계를 명확히 한다

캐릭터의 선택 카드, 계획 카드, 캐릭터의 속마음은 공개 시점이 서로 다르다. UI뿐 아니라 LLM 입력에서도 아직 공개되지 않은 정보가 노출되지 않아야 한다.

미공개 상대 계획의 배치와 소멸은 카드별 묘사 대신 공용의 중립적인 사건으로 전달한다. 카드 이름, 조건, 효과와 카드별 생각은 발동 또는 별도 관찰로 공개되기 전까지 LLM 요청에도 포함하지 않는다.

### 메커니즘 개수는 엔진이 제한하지 않는다

카드는 필요에 따라 여러 메커니즘을 가질 수 있다. 조합의 파워와 가독성은 카드 설계에서 관리하고, 엔진은 조합 순서와 충돌 여부를 일관되게 처리한다.

## 3. 알파 버전의 핵심 범위

알파 버전은 다음 흐름을 처음부터 끝까지 플레이할 수 있어야 한다.

1. 카드 10장을 드래프트해 시작 덱 구성
2. 공개 정보가 다른 캐릭터 3명 중 한 명 선택
3. 제한 턴과 환경 확인
4. 손패 드로우, 캐릭터 행동 태그 공개와 플레이어 카드 선택
5. 사용자가 전송 버튼을 눌러 선택을 확정하고 턴 결과 준비
6. 프리셋을 포함한 정상 요청으로 LLM이 결과를 한 장면으로 묘사
7. 출력 도착 후 턴 결과 반영·공개와 다음 전투 UI 표시
8. 세션 승패 판정 및 보상 선택
9. 덱과 퍽을 변경한 뒤 다음 세션 진행

도감 완성도, 대량의 카드, 복잡한 캐릭터 스토리와 화려한 연출은 이 핵심 흐름이 안정된 뒤 확장한다.

## 4. 확정해야 할 공통 규칙

### 카드 영역

덱, 손패, 버린 카드, 이번 턴 사용 카드, 제거 카드, 계획 슬롯을 구분한다. 같은 턴에 사용한 카드가 다시 섞여 무한 연계되는 것을 막고, 계획과 제거가 일반 버림과 다른 수명을 갖도록 하기 위해 필요하다.

플레이어와 캐릭터의 기본 드로우 수는 3장, 최대 손패는 5장이다. 두 값은 별도 상태로 보존하며 캐릭터별 정의나 효과로 달라질 수 있다. 턴 시작에는 최대치까지 채우는 것이 아니라 기본 드로우 수만큼만 뽑고, 모든 드로우는 최대 손패의 빈자리까지만 허용한다. 카드 효과의 드로우는 해결 즉시 현재 턴 손패에 적용한다.

세션 시작 덱과 버림 더미를 되돌릴 때의 덱은 저장 상태의 고정 시드와 커서를 사용하는 결정적 난수로 섞는다. DB·드래프트 입력 순서가 첫 손패를 고정하지 않으면서도 같은 입력의 판정을 재현하기 위해서다.

### 행동 태그와 메커니즘

모든 카드는 행동 태그를 하나 가진다. 메커니즘은 `연계`, `제거`, `계획`, `간파`부터 시작하며 카드 한 장에 여러 개가 함께 존재할 수 있다.

- 연계: 주 행동을 끝내지 않는 준비 행동
- 제거: 활성 상태에서 벗어난 뒤 이번 세션에서 제외
- 계획: 계획 슬롯에 숨겨진 상태로 배치되어 조건 충족 시 발동
- 간파: 이 카드 때문에 발동할 상대 계획을 이번에만 억제

간파는 계획의 이름, 조건, 효과를 공개하지 않는다. 억제된 계획은 횟수나 충전을 소비하지 않고 숨겨진 상태로 유지하며, 지속시간은 정상적으로 흐른다.

### 은폐 비용과 저항 피해

플레이어 카드는 기본 은폐 비용과 기본 저항 피해를 가질 수 있다. 은폐 비용은 카드를 사용하기 위해 지불하는 자원이며 카드 UI의 좌상단에 양수로 표시한다. 저항 피해는 카드가 기본적으로 감소시키는 상대 저항이며 카드 UI의 우하단에 표시한다. 수치가 없는 경우에는 0을 사용한다.

은폐 비용, 카드나 캐릭터 효과로 인한 은폐 손실, 카드 효과로 인한 은폐 회복은 서로 다른 사건으로 기록한다. 비용 감소 효과와 은폐 손실 감소 효과가 서로 잘못 적용되지 않도록 카드 정의와 실행 로그에서도 이를 구분한다.

환경과 플레이어 퍽은 카드의 은폐 비용을 증가하거나 감소시킬 수 있다. 모든 비용 변경을 적용한 최종 비용은 0보다 작아질 수 없다. 덱 확인 화면에는 카드의 기본 비용을 표시하고, 전투 중 손패에는 현재 환경과 퍽을 반영한 최종 비용을 표시한다.

카드를 사용하려면 현재 은폐가 최종 비용보다 커야 한다. 비용 지불로 은폐가 0 이하가 되는 카드는 사용할 수 없다. 카드 선언 시 최종 은폐 비용을 먼저 지불한 뒤 카드 효과와 트리거를 해결한다.

은폐에는 최대값을 두지 않는다. 회복 효과로 세션 시작 은폐보다 높아질 수 있으며, 시작 은폐는 퍽 등 별도 효과가 없는 경우 30이다.

### 계획

각 진영은 계획 슬롯을 하나만 가진다. 계획은 발동 횟수, 지속시간 또는 별도의 만료 조건 중 하나 이상을 반드시 가진다.

계획이 성공적으로 발동한 뒤 남은 충전이 0이 되면 즉시 버린 카드 더미로 이동한다. 지속시간은 배치 턴에는 줄지 않고 그 다음 턴부터 턴 종료에 1씩 감소하며, 0이 된 계획은 그 턴 종료에 버린다. 간파로 발동이 억제되면 충전은 소비하지 않지만 지속시간은 정상적으로 흐른다. 완료된 전투 상태에는 충전이나 지속시간이 0인 점유 계획을 남기지 않는다.

상대는 슬롯이 차 있다는 사실을 알 수 있고, 지속 턴이 정해진 계획이라면 남은 지속 턴도 알 수 있다. 계획의 이름, 효과와 조건은 실제 발동하거나 관찰 효과로 확인하기 전까지 알 수 없고, 한 번 공개된 계획은 슬롯에서 벗어날 때까지 공개 상태를 유지한다. 남은 발동 횟수는 계획 공개 후에도 숨기며 이를 확인하는 별도 효과가 있을 때만 보여준다. 발동하지 않고 만료되거나 교체된 계획은 사후에도 공개하지 않는다.

### 턴 확정과 결과 공개

전투 UI에서 카드를 클릭하는 동안에는 권위 전투 상태가 아니라 별도 `turnDraft`의 focus, 등록 목록과 결정적 프리뷰만 변경한다. 일반 카드 효과, 캐릭터 행동, 숨은 트리거와 수치 변화는 이 단계에서 판정하거나 적용하지 않는다. 같은 턴 선택을 확장하는 선언형 드로우만 권위 상태와 RNG를 바꾸지 않는 복제 상태에서 미리 계산하며, 플레이어는 전송 전까지 선택한 카드와 연계 순서를 수정할 수 있다.

별도의 `턴 종료` 또는 `행동 확정` 버튼을 두지 않는다. 사용자가 RisuAI의 전송 버튼을 직접 누르는 순간이 이번 턴의 유일한 확정 시점이다. 등록 카드가 없으면 아무 카드도 사용하지 않는 패스, 연계 카드만 있으면 연계를 해결한 뒤 주 행동 없이 패스하는 정상 턴으로 처리한다.

유효한 입력이면 `onInput`에서 턴 시작에 미리 선택된 캐릭터 카드와 플레이어 선택을 함께 해결하고 다음 정보를 가진 대기 트랜잭션을 만든다. 캐릭터 카드의 주 행동 태그는 플레이어 선택 전에 이미 공개되어 있어야 한다.

```text
turnId
beforeState
projectionReceipt
selectedCards
turnResult
afterState
status = awaitingOutput
```

`beforeState.selection.playerCardInstanceIds`는 비워 두고 플레이어의 확정 등록 순서는 `projectionReceipt.selectedCardInstanceIds`와 `selectedCards.player`에 보존한다. 이 영수증은 preview나 workingState를 저장하지 않으며, 대기 View를 만들 때와 추후 결과를 재사용·확정할 때마다 권위 `beforeState`에서 의미 재생 검증한다. `stateSchema`의 pending shape·교차 연결 검증 성공만으로 이 의미 검증을 대신하지 않는다.

`turnResult`가 만들어진 뒤에는 같은 턴을 다시 판정하거나 난수를 다시 사용하지 않는다. 생성 실패, 재시도와 출력 재생성에서도 같은 결과를 사용한다. `afterState`는 정상적인 LLM 출력이 도착하기 전까지 현재 전투 상태로 승격하지 않는다.

이미 `awaitingOutput` 상태라면 다음 `onInput`은 새로운 카드 선택이나 턴을 처리하지 않고 기존 대기 트랜잭션의 묘사만 다시 요청한다.

캐릭터가 실제로 사용한 카드, 저항·은폐 변화, 계획 발동과 최종 무드 결과는 출력 전에 플레이어에게 공개하지 않는다. `editRequest`에서 대기 중인 턴 사건과 묘사 지침을 기존 정상 요청에 추가하고, LLM 출력이 도착하면 `onOutput`에서 `afterState`를 한 번만 반영한다. 장면 묘사와 갱신된 전투 UI가 같은 시점에 캐릭터 행동과 정확한 결과를 공개한다.

같은 해결 시점에 캐릭터 저항과 플레이어 은폐가 모두 0 이하가 되었다면 승리를 우선한다.

### 무드 성과

무드는 턴 전체의 실제 결과를 바탕으로 한 번만 계산한다.

```text
저항 성과 = 턴 시작 저항 - 턴 종료 저항
은폐 소모 = max(0, 턴 시작 은폐 - 턴 종료 은폐)
무드 성과 = 저항 성과 - 은폐 소모
```

무드 성과는 이번 턴에 중간에 발생한 개별 수치를 합산하지 않고 턴 시작과 턴 종료의 최종 저항·은폐 차이로 계산한다. 은폐 비용 지불, 외부 효과로 인한 은폐 손실과 은폐 회복은 모두 이 최종 차이에 반영된다. 은폐 회복은 같은 턴의 은폐 감소를 상쇄할 수 있지만 그 자체로 무드 성과를 양수로 만들지는 않는다. 카드 원문 수치가 아니라 환경, 계획, 특징과 퍽이 반영된 실제 변화량을 사용한다.

기본 무드 경계는 다음 값으로 알파 테스트를 시작한다.

```text
거절 ←5→ 의심 ←4→ 무시 ←4→ 혼란 ←5→ 순응
```

- 양수 기준 이상: 순응 방향으로 한 단계 이동
- 음수 기준 이하: 거절 방향으로 한 단계 이동
- 공통 판정으로는 한 턴에 최대 한 단계만 이동
- 초과 성과는 다음 턴으로 이월하지 않음
- 카드가 직접 무드를 변경하거나 고정했다면 해당 턴의 공통 판정은 생략
- 세션이 종료되었다면 무드 판정은 생략

이 규칙은 카드마다 저항 구간과 무드 이동 조건을 반복해서 작성하지 않도록 하기 위해 사용한다.

## 5. DB와 UI 데이터 전달 계획

### 목적

채팅 변수를 카드명, 저항, 손패 슬롯처럼 세부 항목마다 따로 만들지 않는다. 변수 수가 많아지면 화면 상태를 추적하기 어렵고, 손패 크기나 도감 항목 수가 달라질 때마다 변수와 HTML을 함께 수정해야 하기 때문이다.

대신 화면마다 하나의 구조화된 View 테이블을 만들고 이를 하나의 채팅 변수로 전달한다. HTML은 고정된 레이아웃과 CSS를 담당하고, `dict_element`로 단일 값을 조회하며 CBS 반복문으로 가변 길이 목록을 출력한다.

이 방식은 손패가 효과에 따라 1~5장으로 달라지거나 덱, 도감과 캐릭터 목록의 항목 수가 늘어나도 Lua 변수와 HTML 슬롯을 추가하지 않고 같은 템플릿을 재사용하기 위한 것이다.

### 데이터 흐름과 책임

화면 데이터는 다음 단계를 거쳐 전달한다.

```text
Lua 카드 DB
→ 게임 내부의 카드 정의와 실행 함수
→ 세이브 가능한 런타임 상태와 대기 중인 턴 트랜잭션
→ 화면별 View 테이블
→ toChatVar 직렬화
→ HTML의 dict_element와 CBS 반복문
```

각 단계의 책임은 다음과 같이 구분한다.

- 카드 DB는 카드의 고정 정보와 게임 판정에 필요한 조건·효과 함수를 보유한다.
- 런타임 상태는 카드 ID, 카드가 위치한 영역, 남은 횟수와 임시 변화처럼 플레이 도중 변하는 정보만 보유한다.
- 대기 트랜잭션은 전송 시 결정된 턴 결과와 출력 성공 후 반영할 상태를 보유한다.
- View 테이블은 현재 화면에 실제로 표시할 수 있는 값만 DB와 런타임 상태에서 조합한다.
- `toChatVar`는 View 테이블을 CBS가 읽을 수 있는 채팅 변수 형식으로 변환한다.
- HTML은 View의 값을 출력하고 목록을 반복하지만 게임 규칙을 판정하지 않는다.

카드, 특징, 환경과 캐릭터 같은 정적 게임 정의는 Lua 테이블 DB로 작성한다. 진행 상태와 세이브는 함수가 없는 JSON 직렬화 가능한 데이터만 사용한다. 두 형식 모두 로드 후에는 Lua 테이블이므로 View를 만든 이후에는 같은 `toChatVar` 경로를 사용한다. 정적 Lua DB에는 제한된 로더와 함수가 포함된 원본 테이블의 검증이 필요하다.

### 화면별 View 테이블

하나의 거대한 전역 UI 변수를 모든 화면이 공유하지 않는다. 화면 또는 책임 단위로 최상위 채팅 변수를 나눈다.

- `battleView`: 저항, 은폐, 무드, 턴, 손패, 계획 슬롯과 공개 행동
- `deckView`: 현재 덱, 덱 제한, 보유 카드와 편집 가능 상태
- `collectionView`: 도감 목록, 해금 상태, 필터와 선택 항목
- `profileView`: 캐릭터 공개 정보, 특징과 진행 상태
- `settingsView`: 화면에 표시할 설정값과 선택 항목

각 View는 해당 화면을 열거나 상태가 바뀔 때 다시 생성할 수 있어야 한다. 다른 화면의 데이터까지 함께 넣지 않으며, 카드 DB 전체를 매번 채팅 변수로 복사하지 않는다.

전투 손패처럼 현재 필요한 카드 수가 적은 화면에서는 카드 ID만 전달한 뒤 HTML에서 다시 DB를 조회하게 하지 않는다. View 생성 시 카드명, 설명, 태그, 최종 은폐 비용, 사용 가능 여부처럼 렌더링에 필요한 정보를 조합한 카드 항목을 만든다. 덱과 도감도 같은 원칙으로 현재 필터와 해금 상태가 적용된 표시 목록을 생성한다.

### Lua DB와 직렬화 경계

Lua 카드 DB에는 조건과 효과 함수가 포함될 수 있지만 함수 자체는 채팅 변수나 세이브 데이터에 넣지 않는다. 카드 함수는 읽기 전용 컨텍스트를 받고 상태를 직접 변경하지 않으며 구조화된 효과 명령을 반환한다. 공통 전투 엔진이 명령을 검증하고 보정한 뒤 실제 상태, 이벤트 로그와 LLM 사건에 적용한다. View 생성 과정에는 실행 함수가 아니라 계산이 끝난 표시 결과만 전달한다.

예를 들어 카드의 사용 가능 여부를 결정하는 함수는 내부 DB에 남고, View에는 `playable`과 필요하다면 사용 불가 사유만 기록한다. 카드의 실행 함수, 내부 컨텍스트와 숨겨진 계획 정보가 일반적인 테이블 직렬화 과정에서 우연히 화면으로 유출되지 않도록 허용 필드를 명시적으로 선택한다.

내부 스키마 키와 카드 ID는 안정적인 ASCII 식별자를 사용하고 카드명, 설명, 태그 표시명과 묘사에는 한글을 사용한다. 명칭 변경과 번역이 저장 데이터, 조건 함수와 템플릿 접근 경로를 깨뜨리지 않도록 하기 위한 구분이다.

카드 설명 안의 태그는 `::tag[contact]::` 형식으로 기록한다. View 생성기는 이 토큰을 텍스트와 태그 조각으로 나누고 중앙 표시 레지스트리에서 한글 이름, 태그 종류와 툴팁을 붙인다. 태그 토큰은 화면 표현일 뿐이며 게임 판정이나 카드 보유 태그를 설명 문장에서 역산하지 않는다.

### `toChatVar`의 요구사항

현재의 `toChatVar`와 `toState`는 이 구조를 위한 초기 파싱 도구로 취급한다. 실제 화면 연결 전에 다음 동작을 검증하고 필요한 경우 보완한다.

- 딕셔너리와 순서 있는 목록을 구분해 중첩 구조를 보존한다.
- 손패, 덱과 도감 목록은 직렬화 전후에 순서가 바뀌지 않는다.
- 숫자, 문자열과 불리언이 CBS의 출력 및 조건 비교에서 의도한 의미를 유지한다.
- 함수, 스레드와 직렬화할 수 없는 값은 문자열로 바꾸지 않고 명시적으로 거부하거나 제외한다.
- 중첩된 목록과 딕셔너리를 `dict_element`와 CBS 반복문이 필요한 깊이까지 읽을 수 있다.
- 빈 목록, 빈 딕셔너리와 누락된 값이 서로 구분되어 빈 화면도 오류 없이 렌더링된다.
- 같은 View를 직렬화하면 디버깅 가능한 일관된 결과를 얻는다.

특히 Lua의 `pairs` 순서는 보장되지 않으므로 화면 순서가 중요한 목록은 배열 순서를 보존하거나 명시적인 `order` 또는 `slot`을 사용한다. 숫자와 불리언을 모두 문자열로 바꾸는 경우 CBS 조건문에 미치는 영향도 별도로 확인한다.

### HTML과 CBS 렌더링 원칙

`battleui.html` 같은 HTML 파일에는 화면 골격, CSS, 접근성 정보와 반복 항목 하나의 마크업을 둔다. Lua에서 전투 화면 전체나 카드 목록의 HTML 문자열을 조립하는 방식을 기본으로 사용하지 않는다.

고정 항목은 `dict_element`로 View에서 조회하고, 손패·덱·도감처럼 개수가 달라지는 항목은 CBS 반복문으로 출력한다. 화면에 표시된 버튼에는 목록 항목의 안정적인 카드 ID나 슬롯 번호를 넣어 클릭 이벤트가 다시 Lua 상태를 정확히 가리키게 한다.

카드 설명은 `descriptionSegments`를 순회해 텍스트 조각은 이스케이프하고 태그 조각만 인라인 `span`으로 출력한다. 행동 태그와 메커니즘은 색과 테두리로 구분하고, 중앙 레지스트리의 짧은 정의를 `title` 또는 동등한 접근성 설명으로 제공한다. RisuAI가 CSS 클래스에 붙이는 실제 접두사는 런타임 기준선 단계에서 확인한다.

HTML 조각을 채팅 변수로 직접 전달하는 방식은 CBS만으로 표현하기 어려운 특수 UI가 확인된 경우에만 예외적으로 사용한다. 이 예외도 게임 판정 로직을 포함해서는 안 되며 HTML 이스케이프와 숨겨진 정보 노출 여부를 검토해야 한다.

### 전투 UI 메시지의 수명

전투 UI는 항상 가장 최근의 LLM 출력 아래에 있는 사용자 메시지 하나로 유지한다. 이전 전투 UI를 계속 남기면 모든 UI가 같은 채팅 변수를 읽어 함께 갱신되고 오래된 버튼이 작동할 수 있으므로, 화면에 실제로 존재하는 전투 UI 메시지는 하나여야 한다.

전투 UI 메시지는 위치나 현재 메시지 번호만으로 찾지 않고 전용 마커와 `turnId`로 식별한다. 메시지 추가·삭제로 인덱스가 달라져도 일반 사용자 입력이나 과거 LLM 출력을 잘못 삭제하지 않기 위해서다.

사용자가 전송하면 기존 UI는 대기 상태로 잠그고 모든 카드 버튼을 비활성화한다. UI의 HTML, 버튼과 View 데이터는 모델의 서술 자료가 아니므로 `editRequest`에서 모델 컨텍스트에 포함되지 않게 제외한다. 대신 별도로 만든 턴 사건만 정상 요청에 추가한다.

RisuAI 입력창은 CSS로 입력할 수 없게 만들고 내용은 항상 비워 둔다. 플레이어는 전투 UI에서 카드만 선택하며 정상 요청을 시작하기 위해 전송 버튼을 직접 누른다.

정상 출력이 도착하면 기존 전투 UI 메시지를 제거하고 해당 출력 아래에 새로운 전투 UI 메시지를 추가한다. 새 UI는 방금 끝난 턴의 정확한 결과 요약과 다음 턴의 손패를 함께 보여준다. 출력 생성에 실패하면 기존 UI와 대기 트랜잭션을 유지하고, 재시도 시 같은 `turnResult`를 다시 전달한다.

Lua는 이 과정에서 LLM을 직접 호출하거나 RisuAI의 전송을 대신하지 않는다. 사용자의 수동 전송, RisuAI의 정상 프롬프트 구성과 모델 호출이 끝까지 유지되어야 한다.

### 갱신과 디버깅 원칙

카드 선택 중에는 선택 목록을, 생성 중에는 대기 트랜잭션을, 출력 성공 후에는 확정된 게임 상태를 기준으로 해당 화면의 View를 새로 만든다. View 채팅 변수를 설정한 다음 화면을 렌더링하며, 화면에서 읽은 View 값을 다시 게임의 진실로 사용하지 않는다. 게임 상태, 대기 트랜잭션과 DB가 원본이고 View는 언제든 재생성할 수 있는 출력물이다.

디버그 로그에는 최소한 View 이름, 갱신 이유, 목록 항목 수와 직렬화 성공 여부를 남긴다. 전체 View 내용은 필요한 디버그 레벨에서만 확인해 로그가 카드 설명으로 과도하게 커지지 않게 한다. View 스키마가 바뀔 가능성에 대비해 필요하다면 `schemaVersion`을 두고 HTML 템플릿과의 불일치를 조기에 발견한다.

## 6. 단계별 개발 계획

### 1단계: 용어와 규칙 문서 동기화

`Design.txt`, 카드 아이디어와 실제 구현에서 같은 용어를 사용하도록 정리한다. 특히 계획을 메커니즘으로 변경하고, 대응 키워드를 제거하며, 간파와 숨겨진 계획 규칙을 반영한다.

완료 기준은 하나의 규칙을 서로 다른 문서가 다르게 설명하는 부분이 없는 상태다. 이 단계가 끝나기 전에는 카드 데이터를 대량 작성하지 않는다.

### 2단계: 카드 정의 형식 확정

플레이어와 캐릭터 카드가 공유할 하나의 정의 형식을 만든다. 카드에는 고정 ID, 표시 정보, 행동 태그, 메커니즘 목록, 기본 은폐 비용, 기본 저항 피해, 사용 조건, 효과, 계획 설정과 LLM 묘사가 포함되어야 한다.

정적 정의는 Lua 테이블로 작성하고 조건·효과 함수는 구조화된 효과 명령만 반환한다. 진행 상태와 세이브에는 카드 ID와 변화 가능한 값만 저장하며 실행 함수는 포함하지 않는다.

카드 정의와 실행 중 상태는 분리한다. 같은 카드 두 장, 남은 계획 횟수, 임시 강화와 사용 영역을 안전하게 구분하기 위해 필요하다.

새 메커니즘이 추가되어도 기존 카드 필드 전체를 바꾸지 않아야 한다. 정의를 불러올 때 잘못된 태그, 알 수 없는 메커니즘, 제한 없는 계획과 누락된 묘사를 검사한다.

### 3단계: 카드 영역과 덱 순환 구현

드로우, 사용, 버림, 재섞기, 제거와 계획 배치를 LLM 없이 재현할 수 있게 만든다. 연계로 여러 장을 사용해도 이번 턴 사용 카드가 즉시 덱으로 돌아가지 않아야 한다.

완료 기준은 고정된 카드 목록과 무작위 시드로 여러 턴을 반복했을 때 카드 수가 사라지거나 복제되지 않는 것이다.

### 4단계: 턴 진행과 트리거 순서 구현

캐릭터의 비공개 카드 선택, 주 행동 태그 공개, 플레이어 `turnDraft` 변경, 수동 전송, 대기 트랜잭션 생성, 정상 출력 도착과 상태 반영을 순서대로 구현한다. 카드 클릭은 focus·등록과 공개 선택 단계 프리뷰만 변경하고 사용자의 전송 전에는 일반 효과와 숨은 트리거를 판정하지 않는다.

각 카드의 선언 전, 해결, 해결 후 시점을 구분해야 계획과 간파가 예측 가능한 순서로 작동한다. 자동으로 발동한 계획은 새로운 카드 사용으로 취급하지 않으며 다른 계획을 재귀적으로 발동시키지 않는다.

전송 시에는 선택 projection을 검증해 최소 `projectionReceipt`로 봉인하고, 빈 선택의 `beforeState`, 선택 목록, 확정된 결과와 `afterState`를 묶어 보관한다. 영수증은 결과 재사용·확정 직전에도 다시 재생 검증한다. 출력이 성공한 뒤에만 `afterState`를 반영하고, 같은 `turnId`의 결과는 중복 적용하지 않는다.

완료 기준은 동일한 시작 상태와 선택에서 항상 같은 결과와 이벤트 순서가 발생하며, 생성 실패와 재시도에서도 판정이나 상태 반영이 중복되지 않는 것이다.

### 5단계: 무드 성과 판정 구현

턴 시작 수치와 턴 종료 수치를 기록해 무드 성과를 계산한다. 연계, 캐릭터 행동, 계획, 환경과 퍽으로 변경된 수치를 모두 포함한다.

알파 테스트 로그에는 저항 성과, 은폐 소모, 최종 무드 성과, 요구 기준과 이동 결과를 남긴다. 플레이어가 무드 변화를 이해하지 못하면 전략으로 사용할 수 없기 때문이다.

완료 기준은 문서의 대표 카드 조합을 입력했을 때 예상한 무드 변화가 정확히 발생하는 것이다.

### 6단계: 메커니즘 구현

연계, 제거, 계획, 간파를 각각 독립된 규칙으로 구현한 뒤 조합을 검증한다. 엔진은 메커니즘 개수를 제한하지 않되, 어떤 순서로 개입하는지 일관되게 정한다.

우선 확인할 조합은 `연계+제거`, `계획+제거`, `계획+간파`, `연계+계획`이다. 의도와 다른 결과가 생기는 조합은 카드 설계 금지 목록에 기록하되 엔진 전체의 임의 제한으로 만들지 않는다.

완료 기준은 간파가 자신에게서 파생된 계획 발동만 억제하고, 계획을 공개하거나 충전을 소비하지 않으며, 다른 턴 시작·종료 계획에는 영향을 주지 않는 것이다.

### 7단계: 캐릭터 카드 선택과 정보 공개

버전 1의 캐릭터는 자신의 손패에서 주 행동 한 장을 선택한다. 카드별 총효과 점수는 `저항 회복 + 플레이어 은폐 피해 - 캐릭터 저항 피해 - 플레이어 은폐 회복`이며, 계획은 선택 평가에서 모든 충전이 발동한다고 가정한다. 플레이어 은폐를 0 이하로 만드는 후보가 있으면 그 후보들을 추첨 풀로 사용하고, 아니면 손패 후보 전체를 사용한다. 양수 총효과 점수는 그대로 가중치가 되며, 풀의 모든 점수가 0 이하라면 가장 낮은 점수가 1이 되도록 평행이동한다. 후보가 둘 이상이면 점수가 서로 달라도 고정 시드 가중 추첨을 수행하고, 후보 한 장과 빈 손패 패스만 난수를 소비하지 않는다. 행동 태그 성향 가중치는 사용하지 않는다.

플레이어에게는 선택 카드가 아니라 주 행동 태그만 공개한다. 점수 후보, 계획 발동 가정, 가중치와 난수 영수증은 권위 내부 데이터로 유지한다. 캐릭터 연계 카드의 장수와 순서는 실제 콘텐츠를 추가하기 전에 별도 확정하며, 그전에는 임의로 여러 장을 선택하지 않는다.

캐릭터가 플레이어의 숨겨진 계획 내용을 알고 선택하면 정보전이 무의미해지므로, 선택 판단에는 캐릭터가 알 수 있는 공개 정보만 사용한다.

완료 기준은 같은 상태와 시드에서 항상 같은 카드가 선택되고, 모든 계획 충전과 은폐 치명 후보가 승인된 순서로 평가되며, 숨겨진 카드와 선택 영수증이 UI에 노출되지 않는 것이다.

### 8단계: LLM 장면 묘사 연결

각 카드에는 사용, 계획 배치, 발동과 만료에 필요한 행동·생각 묘사를 기록한다. 엔진은 한 턴의 사건과 최종 수치를 정리해 LLM에 전달한다.

LLM 호출은 사용자가 RisuAI 전송 버튼을 누른 정상 생성 경로에서만 일어난다. Lua는 저수준 LLM 함수를 사용하지 않으며 `editRequest`가 받은 기존 요청을 교체하지 않는다. 프리셋, 캐릭터 설정과 활성 로어북이 포함된 요청에 확정된 턴 사건과 묘사 지침만 추가한다.

숨겨진 캐릭터 계획을 배치할 때는 구체적인 속마음이나 조건을 전달하지 않는다. 공개된 순간에만 관련 묘사를 전달해 카드 UI와 서술이 서로 다른 정보를 주지 않도록 한다.

출력 전에는 캐릭터 카드와 기계적 결과를 UI에 공개하지 않는다. 정상 출력이 도착한 뒤에만 대기 중인 상태를 반영하고, 같은 턴을 재생성할 때는 저장된 결과를 다시 사용한다.

완료 기준은 정상 프리셋 요청을 유지하면서 LLM이 카드 이름이나 규칙 용어를 직접 언급하지 않고, 추가 행동이나 임의의 수치 변화를 만들지 않으며, 여러 연계 행동을 하나의 장면으로 통합하는 것이다.

### 9단계: View 데이터 브리지와 CBS 반복 렌더링

게임 상태와 카드 DB에서 화면별 View 테이블을 생성하고 `toChatVar`를 통해 구조화된 채팅 변수로 전달한다. 먼저 `battleView`로 고정 수치 조회와 가변 손패 반복을 검증한 뒤 같은 방식으로 `deckView`와 `collectionView`를 확장한다.

이 단계에서는 Lua DB의 실행 함수가 View에 포함되지 않는지, 목록 순서와 값의 자료형이 직렬화 전후에 유지되는지, 빈 목록과 1~5장의 손패가 같은 HTML 템플릿에서 정상적으로 표시되는지 확인한다. 선택 중 View는 검증된 `turnDraft`, 출력 대기 View는 검증된 `pendingTurn`과 의미 재생된 projection 영수증을 context로 사용한다. 두 context를 섞지 않고, 출력 대기 중에는 모든 행동 버튼과 focus를 잠근다.

전투 UI 메시지에는 전용 마커와 `turnId`를 부여한다. 정상 출력이 도착할 때 이전 UI만 제거하고 새 출력 아래에 현재 UI를 추가하며, 모델 요청에서는 UI 메시지를 제외한다.

완료 기준은 개별 카드 슬롯용 채팅 변수를 추가하거나 Lua에서 카드 HTML을 조립하지 않고도 전투 손패, 덱과 도감의 가변 목록을 CBS 반복문으로 렌더링하며, 전투 UI 메시지가 항상 하나만 존재하는 것이다.

### 10단계: 전투 UI 완성

플레이어가 판단에 필요한 정보만 빠르게 읽을 수 있도록 저항, 은폐, 현재 무드, 다음 경계값, 남은 턴, 공개된 행동 태그, 양측 계획 슬롯과 카드 영역을 표시한다. 카드에는 좌상단 은폐 비용과 우하단 저항 피해를 일관된 위치에 표시한다. 덱 확인 화면은 기본 비용을, 전투 손패는 환경과 퍽이 반영된 최종 비용을 보여준다.

카드 선택 중에는 focus, 등록 목록, 연계 순서와 프리뷰 카드를 명확히 보여준다. 무선택은 패스, 연계만 선택하면 연계 후 패스로 전송할 수 있다. 출력 대기 중에는 조작을 잠그며 캐릭터 카드와 결과를 미리 공개하지 않는다.

LLM 출력 후에는 무드 성과 계산과 저항·은폐 변화를 짧게 확인할 수 있어야 한다. 숨겨진 계획은 카드 뒷면으로 표시하고 공개된 계획과 명확히 구분한다.

완료 기준은 별도의 가이드 문장을 읽지 않아도 현재 위험, 가능한 행동과 무드 변화 원인을 파악할 수 있는 것이다.

### 11단계: 알파 콘텐츠 제작

공통 규칙이 안정된 뒤 카드와 캐릭터를 늘린다. 첫 알파는 다음 정도로 제한한다.

- 플레이어 카드 약 24장
- 캐릭터 3명
- 캐릭터별 카드 8~10장
- 환경 4개
- 플레이어 퍽 9개 내외

카드 대부분은 행동 태그, 기본 수치와 선택적인 무드 추가효과 하나만 갖도록 한다. 직접 무드를 변경하는 카드는 소수의 핵심 카드로 제한한다.

### 12단계: 밸런스 및 플레이 로그 분석

알파 세션마다 턴 수, 사용 카드, 저항·은폐 변화, 무드 이동, 계획 발동·억제, 승패 원인과 사용되지 않은 카드를 기록한다.

다음 질문에 답할 수 있을 때까지 수치를 조정한다.

- 일반 세션이 의도한 7~12턴 안에 끝나는가?
- 무드가 지나치게 고정되거나 매턴 왕복하지 않는가?
- `5-4-4-5` 경계값이 카드 수치와 맞는가?
- 간파가 계획 덱을 무력화하지 않으면서 한 턴의 가치가 있는가?
- 덱이 20장에 가까워졌을 때 필요한 카드가 지나치게 나오지 않는가?
- 특정 태그나 메커니즘 조합이 명백한 정답이 되지 않는가?

## 7. 필수 검증 항목

- 같은 무작위 시드와 선택은 같은 기계적 결과를 만든다.
- 사용 카드와 제거 카드가 같은 턴에 다시 드로우되지 않는다.
- 계획은 제한 없이 슬롯에 남지 않는다.
- 간파는 관련 계획만 억제하며 계획 정보를 공개하지 않는다.
- 메커니즘 조합 순서가 카드 데이터의 배열 순서에 따라 달라지지 않는다.
- 카드의 직접 무드 변경과 공통 무드 판정이 의도치 않게 중복되지 않는다.
- 환경과 퍽이 적용된 최종 은폐 비용은 0보다 작아지지 않는다.
- 현재 은폐 이하로 지불할 수 없는 카드는 사용 불가 상태가 되며 비용이 효과보다 먼저 처리된다.
- 은폐 비용, 은폐 손실과 은폐 회복이 서로 다른 사건으로 기록되면서도 무드 성과는 턴 시작과 종료의 최종 차이로 계산된다.
- 저항이나 은폐가 0이 되면 남은 행동을 중단한다.
- 같은 확인 시점에 저항과 은폐가 모두 0 이하라면 승리로 처리한다.
- 숨겨진 계획 정보가 LLM 묘사로 유출되지 않는다.
- 카드 설명과 실제 효과가 다를 경우 개발 단계에서 발견할 수 있다.
- 세이브 데이터에는 실행 함수가 아니라 카드 ID와 변화 가능한 상태만 저장한다.
- 화면별 View에는 실행 함수와 비공개 정보가 포함되지 않는다.
- 손패가 0~5장으로 달라져도 같은 CBS 템플릿이 목록을 정상적으로 렌더링한다.
- 덱과 도감의 항목 수가 달라져도 개별 슬롯용 채팅 변수를 추가하지 않는다.
- View의 순서 있는 목록은 `toChatVar` 변환 전후에 순서가 유지된다.
- View의 숫자와 불리언은 CBS 조건 및 비교에서 의도한 자료형 의미를 유지한다.
- 게임 상태에서 같은 View를 다시 생성할 수 있으며 채팅 변수의 값을 게임 판정 원본으로 사용하지 않는다.
- 카드 선택은 별도 `turnDraft`만 변경한다. 선택 단계 드로우는 권위 상태와 RNG를 바꾸지 않는 결정적 프리뷰로 계산하고 일반 카드 효과와 숨은 트리거는 전송 전에 판정하지 않는다.
- 사용자가 직접 누르는 RisuAI 전송 버튼만 턴을 확정하고 LLM 생성을 시작한다.
- RisuAI 입력창은 비어 있고 입력할 수 없으며 플레이어의 게임 입력은 전투 UI의 카드 선택으로 제한된다.
- Lua가 저수준 LLM 함수를 호출하거나 전송을 자동화하지 않는다.
- `editRequest`는 프리셋을 포함한 정상 요청을 보존하고 확정된 턴 사건만 추가한다.
- 등록 카드가 없는 전송과 연계 카드만 있는 전송도 각각 패스와 연계 후 패스로 정상 진행된다.
- 대기 트랜잭션의 projection 영수증은 shape 검증과 별도로 권위 `beforeState`에서 재생 검증되며 preview 선택 순서가 권위 selection을 오염시키지 않는다.
- 캐릭터 카드와 정확한 턴 결과는 정상 출력이 도착하기 전에 공개되지 않는다.
- 생성 실패, 재시도와 출력 재생성은 같은 `turnResult`를 사용하며 효과와 난수를 다시 처리하지 않는다.
- 같은 `turnId`의 `afterState`는 정상 출력 뒤 한 번만 반영된다.
- 전투 UI 메시지는 항상 하나만 존재하고 전용 마커로 식별되며 모델 요청에서는 제외된다.

## 8. 주요 위험과 대응

### 무드 계산이 플레이어에게 불투명할 위험

턴 결과 로그에 계산 과정을 보여주고 무드 UI에 다음 상승·하락 기준을 표시한다. 카드 선택 시 확정 가능한 자신의 기본 효과는 예상값으로 보여주는 것도 검토한다.

### 숨겨진 계획이 무작위 처벌처럼 느껴질 위험

계획 슬롯의 존재와 남은 지속시간은 공개하고, 관찰과 간파를 서로 다른 대응 수단으로 제공한다. 남은 발동 횟수는 공개하지 않으며 발동하지 않고 만료·교체된 계획도 공개하지 않는다. 간파 사용 후에는 내용 없이 계획 발동이 억제되었다는 사실만 알려준다.

### 메커니즘 조합이 급격히 복잡해질 위험

엔진은 조합을 허용하되 카드 제작 시 조합별 테스트를 요구한다. 복잡한 조합은 희귀 카드에 한정하고, 플레이 화면에서는 실제 적용되는 메커니즘을 모두 표시한다.

### LLM이 한 턴에 너무 많은 사건을 만드는 위험

연계 카드의 묘사를 짧은 준비 행동으로 제한하고, 메인 행동과 캐릭터 반응을 중심으로 한 장면을 구성한다. 기계적으로 발생하지 않은 사건을 추가하지 말라는 지침을 유지한다.

### 구조화된 채팅 변수가 지나치게 커질 위험

화면별 View에는 현재 화면에 필요한 항목만 넣고 카드 DB 전체나 다른 화면의 데이터를 복사하지 않는다. 목록이 커지는 도감은 필터, 페이지 또는 구간 단위 View 생성을 검토하며, 같은 카드의 실행 함수와 내부 상태는 전달하지 않는다.

### DB와 화면 스키마가 서로 강하게 결합될 위험

HTML이 카드 DB를 직접 해석하지 않고 View 스키마만 사용하도록 한다. 카드 DB 구조가 바뀌면 View 생성 과정에서 변환하고, HTML의 접근 경로와 CBS 반복 구조는 가능한 한 유지한다.

### 생성 실패로 턴 상태가 어긋날 위험

전송 시 현재 상태를 즉시 덮어쓰지 않고 `beforeState`, `turnResult`와 `afterState`를 대기 트랜잭션으로 보관한다. 출력이 도착하면 한 번만 반영하고, 실패하거나 재시도할 때는 같은 결과를 재사용한다.

### 전투 UI 메시지 이동이 채팅 기록을 손상할 위험

전투 UI를 마지막 사용자 메시지라는 위치만으로 찾지 않고 전용 마커와 `turnId`로 식별한다. 일반 사용자 입력과 LLM 출력은 삭제하지 않으며, UI 메시지만 모델 요청에서 제외하고 정상 출력 뒤 교체한다.

## 9. 알파 완료 기준

다음 조건을 모두 만족하면 핵심 시스템 알파가 완료된 것으로 본다.

- 최소 세 번의 연속 세션을 중단 없이 플레이할 수 있다.
- 덱 구성, 캐릭터 선택, 전투, 보상과 다음 세션이 연결된다.
- LLM을 끈 상태에서도 모든 규칙과 승패가 정상 작동한다.
- LLM을 켰을 때 동일한 기계적 결과를 훼손하지 않고 장면이 생성된다.
- 숨겨진 계획, 간파, 연계, 제거와 무드 판정의 대표 조합이 검증된다.
- 플레이 로그만으로 예상치 못한 결과의 원인을 추적할 수 있다.
- 알파 카드 수치를 데이터 수정만으로 반복 조정할 수 있다.
- 전투, 덱과 도감의 가변 목록을 화면별 View와 CBS 반복문으로 표시할 수 있다.
- 사용자의 수동 전송부터 정상 프리셋 요청, 결과 묘사, 상태 반영과 새 전투 UI 표시까지 한 흐름으로 완료된다.
- 생성 실패와 재시도 후에도 같은 턴 결과가 중복 적용되거나 다시 판정되지 않는다.

이 기준을 달성한 뒤 카드 풀, 캐릭터 고유 규칙, 환경, 퍽과 도감 콘텐츠를 확장한다.
