# 신규 캐릭터 생성 가이드

이 가이드는 HellTrain **소스 폴더를 받은 플레이어**가 새 상대 캐릭터를 추가해 자기 배포본을 만드는 방법입니다. 완성된 `.charx` 파일만으로는 DB를 편집할 수 없습니다. 새 캐릭터는 전용 카드가 10장 미만이면 공용 카드로 10장을 채우고, 10장 이상이면 전용 카드만 사용합니다.

## 1. 파일과 ID 정하기

예시에서는 캐릭터 ID를 `custom_aya`, DB 파일명을 `CustomAya.db`로 사용합니다.

- ID는 영문 소문자로 시작하고 영문 소문자·숫자·밑줄만 사용합니다. 다른 캐릭터 및 카드 ID와 겹치면 안 됩니다.
- DB 파일명은 영문자로 시작하고 영문자·숫자·밑줄 뒤에 `.db`를 붙입니다.
- 캐릭터 한 명당 `Char/` 아래에 DB 파일 하나를 만듭니다.
- 이 프로젝트는 성인 캐릭터를 전제로 하므로 공개 나이를 20세 이상으로 설정합니다.

## 2. 캐릭터 목록에 등록하기

`Char/CharacterList.db`의 `characters` 안에 다음 항목을 추가합니다. `name`과 `turnLimit`은 다음 단계의 개별 DB와 **정확히 같아야** 합니다.

```lua
custom_aya = {
    id = "custom_aya",
    database = "CustomAya.db",
    name = "아야",
    turnLimit = 9,
},
```

모든 캐릭터가 같은 덱 규칙을 사용하므로 별도의 카드 풀 지정 필드는 필요하지 않습니다.

`Char/`에 넣은 개별 캐릭터 `.db` 파일은 모두 이 목록에 등록해야 합니다. 목록에만 있고 파일이 없거나, 파일에 목록의 캐릭터 ID 정의가 없어도 검사 오류가 납니다. 등록 캐릭터 수에는 고정 제한을 두지 않습니다.

## 3. 캐릭터 DB 만들기

`Char/CustomAya.db` 파일을 만들고 아래 내용을 넣은 뒤 프로필과 수치를 수정합니다. Lua 파일처럼 실행되는 DB이므로 쉼표, 따옴표, 중괄호를 유지하십시오.

```lua
return {
    schemaVersion = 1,
    kind = "characterDatabase",
    characters = {
        custom_aya = {
            id = "custom_aya",
            name = "아야",
            portraitImage = "아야.png",
            fallenImage = "아야_함락.png",
            publicProfile = {
                age = 27,
                occupation = "회사원",
                appearance = {
                    hairColor = "검은색",
                    eyeColor = "갈색",
                    skin = "밝은 피부",
                    height = 166,
                    weight = 54,
                    style = "단정한 출근복",
                },
                personality = {
                    traits = { "신중함", "단호함" },
                    likes = { "독서", "조용한 장소" },
                },
                background = {
                    family = "혼자 거주함",
                    hobbies = "독서",
                    boardingReason = "퇴근 후 귀가하는 길",
                },
            },
            sexualPreference = "서로의 경계를 존중하며 천천히 신뢰를 쌓는 관계를 선호함",
            backgroundNarrative = "차분한 일상을 보내던 회사원으로, 퇴근 후 집으로 돌아가던 중 낯선 사건에 휘말렸다.",
            battle = {
                startingResistance = 30,
                turnLimit = 9,
                startingMood = "suspicion",
                baseDrawCount = 3,
                maxHandSize = 5,
                planCapacity = 1,
                traitIds = {},
                deck = {},
            },
        },
    },
    cards = {},
}
```

`battle.deck`이 비어 있어도 공용 기본 덱이 자동으로 적용됩니다. 로더는 모든 캐릭터의 `battle.deck`을 전용 카드 목록으로 취급하고, `DB/CommonCharacterCards.db`의 `deck`을 보충 후보 풀로 사용합니다. 전투 시작 시 전용 카드가 10장 미만이면 전투 시드로 공용 풀을 섞고, 부족한 수만큼 중복 없이 뽑아 최종 10장을 만듭니다. 같은 시드는 같은 구성을 재현하며, 그 뒤에 실제 뽑기 순서도 섞입니다.

초상화와 함락 이미지는 `imgs/` 폴더에 넣고 DB의 `portraitImage`, `fallenImage`에는 **경로를 제외한 파일명만** 적습니다. 파일명은 실제 이미지와 정확히 같아야 합니다. 어느 한 필드라도 없거나 비어 있으면 두 이미지 모두 포함된 기본 이미지 `dummy.png`로 표시됩니다.

`startingMood`는 `rejection`, `suspicion`, `ignore`, `confusion`, `compliance` 중 하나입니다. `turnLimit`은 7~12, `startingResistance`는 양수, `baseDrawCount`와 `maxHandSize`는 양의 정수이며 드로우 수가 최대 손패보다 클 수 없습니다. `planCapacity`는 1~16입니다. `traitIds`에 특징을 넣으려면 `DB/CharTraits.db`에 등록된 캐릭터 특징 ID를 사용합니다.

## 4. 전용 카드 추가하기

전용 카드가 필요하면 같은 DB의 `cards`에 정의하고, 그 ID를 `battle.deck`에 추가합니다. 아래는 **구조 예시**입니다. 카드의 효과와 묘사는 캐릭터에 맞게 바꾸십시오.

```lua
-- battle 안
deck = { "custom_aya_guard" },

-- 파일 최상위 cards 안
custom_aya_guard = characterCardSupport.card(
    "custom_aya_guard", "침착한 방어", { "recovery" }, {}, { "저항 2 회복." },
    "호흡을 고르고 자세를 바로잡습니다.",
    {
        actorAction = "손잡이를 단단히 잡고 몸을 돌린다.",
        actorThought = "침착하게 대응하자.",
    },
    function() return { characterCardSupport.recoverResistance(2) } end
),
```

`deck = {}`이면 공용 풀에서 10장을 뽑습니다. 레이무처럼 전용 카드 한 장을 적으면 그 카드와 공용 카드 9장으로 구성됩니다. 전용 카드가 정확히 10장이면 공용 카드를 쓰지 않고, 10장을 넘으면 전용 카드 전체를 그대로 사용합니다. 공용 풀의 고유 카드 수가 필요한 보충 수보다 적으면 검증 오류가 납니다.

`deck`에는 **이 캐릭터 DB의 `cards`에 정의한 카드 ID만** 넣을 수 있습니다. 공용 카드나 다른 캐릭터의 카드 ID를 직접 참조하면 검증 오류가 납니다. `cards`에 정의했지만 `deck`에 넣지 않은 카드는 전투 덱에 포함되지 않습니다. 공용 덱과 공용 카드 정의는 `DB/CommonCharacterCards.db`에 있으며, 카드 생성·효과 함수는 `DB/CharacterCardSupport.db`에 있습니다. 기존 캐릭터 DB의 카드 정의도 작성 예시로 참고할 수 있습니다.

## 5. 검사하고 배포본 만들기

프로젝트 루트에서 PowerShell로 실행합니다. 처음 빌드하는 컴퓨터라면 먼저 `build/setup.bat`을 실행합니다.

```powershell
.\build\setup.bat
.\build\release.bat
```

전체 검사가 통과하면 `build/HellTrain.charx`가 생성됩니다. 이것을 RisuAI에 가져와 캐릭터 선택 화면에 새 캐릭터가 나타나는지, 전투가 시작되는지 확인하십시오. 검사 오류가 나면 출력된 DB 이름과 필드 경로부터 수정합니다. 단순히 DB 파일만 추가하고 기존 `.charx`를 그대로 배포하면 새 캐릭터가 포함되지 않습니다.

## 자주 확인할 오류

| 증상 | 확인할 곳 |
|---|---|
| 캐릭터를 찾을 수 없음 | `CharacterList.db` 등록, DB 파일명, 캐릭터 ID 일치 여부 |
| 목록과 정의 불일치 | 목록과 DB의 `name`, `turnLimit` 일치 여부 |
| 공용 카드 풀 오류 | `DB/CommonCharacterCards.db`의 `deck`과 카드 정의, 보충에 필요한 카드 수 |
| 덱 카드 오류 | `battle.deck` ID가 같은 DB의 `cards`에 있는지, 중복인지 |
| 캐릭터 선택 화면 오류 | `publicProfile.age`, `occupation`, `appearance`의 표시 정보 |
| 이미지가 보이지 않음 | `imgs/`의 실제 파일명, DB의 `portraitImage`·`fallenImage` 값, 배포본 재빌드 여부 |

모든 캐릭터 DB가 동일하게 `battle.deck` 방식을 사용합니다. 덱이 10장 미만이면 별도 설정 없이 공용 카드가 자동으로 보충됩니다.

실제 작성 예시는 `Char/HakureiReimu.db`를 참고하십시오. 레이무 예시는 `reimu_boundary_guard` 한 장과 전투 시드로 선택한 공용 카드 9장으로 10장 덱을 만듭니다. 두 이미지 필드는 빈 문자열로 두어 `dummy.png` 대체 동작을 보여 줍니다.
